# Breaking 训练卡片项目 Spec

## 1. 项目概述

这是一个面向手机端的个人 Breaking 训练网页。

核心使用场景：

- 在 iPhone 上打开
- 按当天训练计划执行
- 进行计时训练、计数训练和勾选训练
- 用滑动卡片记录完成状态
- 在浏览器本地保存训练记录
- 通过日历查看历史训练
- 使用内置测试模式快速验证交互流程

这是一个本地优先、轻量执行型工具。

不需要：

- 后端
- 账号系统
- API 密钥

---

## 2. 产品目标

这个项目的目标是帮助用户：

- 执行结构化的 Breaking 周训练计划
- 聚焦 flow、freeze 控制、结构能力和 swipe 基础
- 尽量减少训练中复杂录入
- 保持单手、手机友好的交互方式
- 不离开应用即可复盘过往日期

这个项目不是：

- 通用健身打卡器
- Power 动作专项训练平台
- 社交产品

---

## 3. 当前训练定位

当前训练阶段：

- 舞蹈连接能力建立期

当前优先级：

1. Freestyle
2. Freeze 控制
3. 倒立结构
4. Swipe 基础
5. Power 前置能力

应用需要强化的方向：

- 重连接，不重动作收藏
- 重输出，不重无序练习
- 重稳定控制，不重难度堆积

---

## 4. 周计划模型

### 周一

- 结构控制日
- 模块：
  - 热身
  - 膝盖维护
  - 核心训练
  - Freeze 训练
  - 倒立结构
  - 拉伸

### 周二

- 课程输出日
- 模块：
  - 课程输出

### 周三

- Freestyle 主训练日
- 模块：
  - 热身
  - Toprock 专项
  - Freestyle 主训练
  - Freeze 融入
  - 放松

### 周四

- 灵活训练日
- 模块：
  - 恢复 / 轻技术训练

### 周五

- Swipe 专项日
- 模块：
  - 核心训练
  - Swipe 结构专项
  - 连接训练
  - 拉伸

### 周六

- 舞蹈整合日
- 模块：
  - 热身
  - Round 训练
  - 问题修正
  - 创作时间
  - 放松

### 周日

- 恢复日
- 模块：
  - 恢复勾选

---

## 5. 模块类型

应用目前支持三类模块。

### 5.1 勾选模块

用于只需要提醒完成的训练内容。

例如：

- 热身
- Freeze 融入
- 课程输出
- Round 训练
- 恢复日

行为：

- 以 checkbox 勾选形式完成
- 保存在本地
- 计入当天完成度

### 5.2 步骤模块

用于包含多个训练动作的专项模块。

例如：

- 膝盖训练
- 倒立结构
- Swipe 结构专项

行为：

- 每个动作是一个训练卡片
- 动作可以是计时类或计数类
- 右滑表示完成
- 左滑表示未完成
- 模块内动作之间可以插入休息

### 5.3 核心模块

这是一个特殊的步骤模块。

行为：

- 总共 4 组
- 每组 6 个动作
- 动作从现有核心动作池中抽取
- 每一组都应该独立生成
- 每一组动作顺序不应完全重复

---

## 6. 动作类型

### 6.1 计时动作

例如：

- 墙静蹲
- 墙倒立
- 单手支撑

行为：

- 显示倒计时
- 点击卡片开始
- 普通专项默认不自动开始
- 即使未跑完时间，也可以通过滑动提前记录

### 6.2 计数动作

例如：

- 离心深蹲
- 倒立肩上提
- 重心转换
- 二步扫腿训练

行为：

- 显示为计数训练
- 不强制走倒计时
- 通过用户自己的判断滑动记录

---

## 7. 卡片交互模型

### 训练卡片行为

- 大面积全屏训练卡
- 点击卡片开始 / 暂停计时
- 用滑动完成结果记录

### 滑动规则

- 第一次滑动：卡片移动到一侧，作为确认
- 第二次同方向滑动：确认动作
- 卡片飞出屏幕
- 中间出现状态反馈

### 当前完成逻辑

- 右滑：完成
- 左滑：未完成

例外情况：

- 休息阶段使用左滑提前结束休息，进入下一项

### 休息卡行为

- 出现在步骤模块的动作之间
- 显示短暂休息倒计时
- 显示下一个动作名称，方便提前准备
- 底部有圆形 `+10s` 按钮

---

## 8. 计时规则

### 普通专项模块

- 进入模块后不自动开始动作
- 用户点击后才开始计时
- 计数动作保持手动记录

### 核心模块

- 核心模块允许更强的自动流程
- 动作之间有休息
- 休息卡展示下一个动作

### 休息规则

- 专项训练模块有组间休息
- 用户可以提前结束休息
- 用户可以点击 `+10s`

---

## 9. 数据模型

所有数据保存在浏览器 `localStorage` 中。

### 每日训练记录 key

`breaking-full-card-YYYY-MM-DD`

### 应用设置 key

`breaking-app-settings`

### 主状态结构

```json
{
  "version": 6,
  "startedAt": 0,
  "finishedAt": 0,
  "total": 0,
  "modules": {},
  "notes": "",
  "current": null
}
```

### 模块状态结构

```json
{
  "index": 0,
  "done": 0,
  "miss": 0,
  "checks": {},
  "phase": "work",
  "remaining": 0,
  "steps": []
}
```

必须保留版本字段，因为训练结构会持续调整。
当版本不匹配时，应当丢弃不兼容的旧模块数据，避免旧训练结构污染新计划。

---

## 10. 测试模式

应用有两种模式：

- 正式执行
- 测试模式

### 正式执行

- 使用正常训练时长
- 默认以今天作为当前训练日

### 测试模式

- 缩短计时，便于调试
- 允许通过日历切换查看某一天

### 测试模式下的日历行为

- 点击日历中的某一天，会把应用切换到该天进行查看
- 这个切换只用于查看，不是正式修改当前日期

### 非测试模式下的日历行为

- 点击日期只显示详情
- 不切换当前训练日期

---

## 11. 日历与历史记录

日历需要支持：

- 月视图网格
- 每天状态颜色
- 某一天的详情面板

### 状态颜色

- 蓝色：恢复日
- 绿色：已完成
- 红色：未完成
- 灰色：未开始

### 某日详情面板

应展示：

- 日期
- 当天标题
- 模块列表
- 每个模块的进度概览

---

## 12. 强度系统 Spec

这部分是计划中的功能，暂未完整实现。

### 目标

按训练模块整体调整强度，而不是按单个动作调整。

### 适用范围

- 核心训练
- Swipe 结构专项
- 倒立结构
- 膝盖训练

### 数据结构

```json
{
  "intensity": {
    "core": 1,
    "swipeStructure": 1,
    "handstand": 1,
    "knee": 1
  }
}
```

### 模块结束后的反馈项

- 太高
- 正常
- 太低

### 调整规则

- 太高：下次该模块 `-10%`
- 太低：下次该模块 `+10%`
- 正常：逐步回到 `100%`

### 限制范围

- 最低：`70%`
- 最高：`120%`

### 调整方式

- 计时动作：按系数缩放秒数
- 计数动作：按系数缩放次数
- 组数默认不动，只有强度较低时才考虑减组

这部分必须按“模块整体”实现，不拆到单个动作。

---

## 13. 技术架构

### 前端

- 纯静态 HTML
- 独立 CSS 文件
- 独立 JavaScript 文件

### 本地开发

- `server.js` 提供静态资源服务
- `npm run serve` 启动本地服务

### 部署目标

- GitHub 仓库
- Vercel 静态部署

当前目录包含：

- `index.html`
- `assets/app.css`
- `assets/app.js`
- `server.js`
- `vercel.json`

---

## 14. 设计原则

### 交互原则

- 手机优先
- 卡片优先
- 尽量低阻力
- 大面积点击区域

### 视觉方向

- 白色基础卡片
- 训练中卡片使用蓝色
- 休息卡片使用黄色
- 暂停时回到白色

### 执行体验

- 训练中不要出现过多文字
- 只在真正有帮助时显示提示和下一步
- 降低用户录入负担
- 让进度可以一眼读懂

---

## 15. 当前已知待办

这些是后续建议继续处理的点：

- 全面检查并统一 UTF-8 编码，避免中文文案乱码
- 继续验证计数动作在所有滑动路径下的首页计数正确性
- 细化专项训练中的组间休息逻辑
- 增加模块级强度反馈 UI
- 进一步验证测试模式切换日期后的缓存行为
- 确认 GitHub Pages 和 Vercel 的最终发布流程

---

## 16. 在新会话中的使用方式

如果另一个 Codex 会话需要继续这个项目，建议：

1. 先拉取 `outputs` 目录中的现有框架
2. 以这份 spec 作为当前产品事实标准
3. 没有明确要求时，不主动改变现有卡片交互模型
4. 把强度系统视为下一阶段功能，而不是当前已完成项
5. 在大改 UI 前，优先做一次文字编码与文案清理
